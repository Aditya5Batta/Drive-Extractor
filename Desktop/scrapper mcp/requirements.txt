# MCP framework
mcp>=1.2.0

# HTTP client
httpx>=0.27.0

# HTML parsing / main-content extraction
trafilatura>=1.12.0
beautifulsoup4>=4.12.0
lxml>=5.2.0

# PDF extraction
pdfminer.six>=20240706
pypdf>=5.0.0

# PDF multimodal extraction — tables (pdfplumber) and figures (PyMuPDF)
# Both are optional: server.py feature-gates and runs fine without them.
pdfplumber>=0.11.0
pymupdf>=1.24.0

# Word (.docx) report generation in Times New Roman
python-docx>=1.1.0

# Used by mcp for URI validation
pydantic>=2.7.0
